# Upper bounds keep a surprise release from breaking the live deploy for the
# community — Railway reinstalls these fresh on every push.
fastapi>=0.141.1,<1.0
uvicorn[standard]>=0.52.1,<1.0
httpx>=0.28.1,<0.29
